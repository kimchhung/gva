// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/gva/app/database/schema/pulid"
	"github.com/gva/internal/ent/comicchapter"
	"github.com/gva/internal/ent/comicimg"
	"github.com/gva/internal/ent/predicate"

	"github.com/gva/internal/ent/internal"
)

// ComicImgQuery is the builder for querying ComicImg entities.
type ComicImgQuery struct {
	config
	ctx         *QueryContext
	order       []comicimg.OrderOption
	inters      []Interceptor
	predicates  []predicate.ComicImg
	withChapter *ComicChapterQuery
	withFKs     bool
	loadTotal   []func(context.Context, []*ComicImg) error
	modifiers   []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ComicImgQuery builder.
func (ciq *ComicImgQuery) Where(ps ...predicate.ComicImg) *ComicImgQuery {
	ciq.predicates = append(ciq.predicates, ps...)
	return ciq
}

// Limit the number of records to be returned by this query.
func (ciq *ComicImgQuery) Limit(limit int) *ComicImgQuery {
	ciq.ctx.Limit = &limit
	return ciq
}

// Offset to start from.
func (ciq *ComicImgQuery) Offset(offset int) *ComicImgQuery {
	ciq.ctx.Offset = &offset
	return ciq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ciq *ComicImgQuery) Unique(unique bool) *ComicImgQuery {
	ciq.ctx.Unique = &unique
	return ciq
}

// Order specifies how the records should be ordered.
func (ciq *ComicImgQuery) Order(o ...comicimg.OrderOption) *ComicImgQuery {
	ciq.order = append(ciq.order, o...)
	return ciq
}

// QueryChapter chains the current query on the "chapter" edge.
func (ciq *ComicImgQuery) QueryChapter() *ComicChapterQuery {
	query := (&ComicChapterClient{config: ciq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := ciq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := ciq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(comicimg.Table, comicimg.FieldID, selector),
			sqlgraph.To(comicchapter.Table, comicchapter.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, comicimg.ChapterTable, comicimg.ChapterColumn),
		)
		schemaConfig := ciq.schemaConfig
		step.To.Schema = schemaConfig.ComicChapter
		step.Edge.Schema = schemaConfig.ComicImg
		fromU = sqlgraph.SetNeighbors(ciq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ComicImg entity from the query.
// Returns a *NotFoundError when no ComicImg was found.
func (ciq *ComicImgQuery) First(ctx context.Context) (*ComicImg, error) {
	nodes, err := ciq.Limit(1).All(setContextOp(ctx, ciq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{comicimg.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ciq *ComicImgQuery) FirstX(ctx context.Context) *ComicImg {
	node, err := ciq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ComicImg ID from the query.
// Returns a *NotFoundError when no ComicImg ID was found.
func (ciq *ComicImgQuery) FirstID(ctx context.Context) (id pulid.ID, err error) {
	var ids []pulid.ID
	if ids, err = ciq.Limit(1).IDs(setContextOp(ctx, ciq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{comicimg.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ciq *ComicImgQuery) FirstIDX(ctx context.Context) pulid.ID {
	id, err := ciq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ComicImg entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ComicImg entity is found.
// Returns a *NotFoundError when no ComicImg entities are found.
func (ciq *ComicImgQuery) Only(ctx context.Context) (*ComicImg, error) {
	nodes, err := ciq.Limit(2).All(setContextOp(ctx, ciq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{comicimg.Label}
	default:
		return nil, &NotSingularError{comicimg.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ciq *ComicImgQuery) OnlyX(ctx context.Context) *ComicImg {
	node, err := ciq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ComicImg ID in the query.
// Returns a *NotSingularError when more than one ComicImg ID is found.
// Returns a *NotFoundError when no entities are found.
func (ciq *ComicImgQuery) OnlyID(ctx context.Context) (id pulid.ID, err error) {
	var ids []pulid.ID
	if ids, err = ciq.Limit(2).IDs(setContextOp(ctx, ciq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{comicimg.Label}
	default:
		err = &NotSingularError{comicimg.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ciq *ComicImgQuery) OnlyIDX(ctx context.Context) pulid.ID {
	id, err := ciq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ComicImgs.
func (ciq *ComicImgQuery) All(ctx context.Context) ([]*ComicImg, error) {
	ctx = setContextOp(ctx, ciq.ctx, "All")
	if err := ciq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ComicImg, *ComicImgQuery]()
	return withInterceptors[[]*ComicImg](ctx, ciq, qr, ciq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ciq *ComicImgQuery) AllX(ctx context.Context) []*ComicImg {
	nodes, err := ciq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ComicImg IDs.
func (ciq *ComicImgQuery) IDs(ctx context.Context) (ids []pulid.ID, err error) {
	if ciq.ctx.Unique == nil && ciq.path != nil {
		ciq.Unique(true)
	}
	ctx = setContextOp(ctx, ciq.ctx, "IDs")
	if err = ciq.Select(comicimg.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ciq *ComicImgQuery) IDsX(ctx context.Context) []pulid.ID {
	ids, err := ciq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ciq *ComicImgQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ciq.ctx, "Count")
	if err := ciq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ciq, querierCount[*ComicImgQuery](), ciq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ciq *ComicImgQuery) CountX(ctx context.Context) int {
	count, err := ciq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ciq *ComicImgQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ciq.ctx, "Exist")
	switch _, err := ciq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ciq *ComicImgQuery) ExistX(ctx context.Context) bool {
	exist, err := ciq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ComicImgQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ciq *ComicImgQuery) Clone() *ComicImgQuery {
	if ciq == nil {
		return nil
	}
	return &ComicImgQuery{
		config:      ciq.config,
		ctx:         ciq.ctx.Clone(),
		order:       append([]comicimg.OrderOption{}, ciq.order...),
		inters:      append([]Interceptor{}, ciq.inters...),
		predicates:  append([]predicate.ComicImg{}, ciq.predicates...),
		withChapter: ciq.withChapter.Clone(),
		// clone intermediate query.
		sql:  ciq.sql.Clone(),
		path: ciq.path,
	}
}

// WithChapter tells the query-builder to eager-load the nodes that are connected to
// the "chapter" edge. The optional arguments are used to configure the query builder of the edge.
func (ciq *ComicImgQuery) WithChapter(opts ...func(*ComicChapterQuery)) *ComicImgQuery {
	query := (&ComicChapterClient{config: ciq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	ciq.withChapter = query
	return ciq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"createdAt,omitempty" rql:"filter,sort"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ComicImg.Query().
//		GroupBy(comicimg.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ciq *ComicImgQuery) GroupBy(field string, fields ...string) *ComicImgGroupBy {
	ciq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ComicImgGroupBy{build: ciq}
	grbuild.flds = &ciq.ctx.Fields
	grbuild.label = comicimg.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"createdAt,omitempty" rql:"filter,sort"`
//	}
//
//	client.ComicImg.Query().
//		Select(comicimg.FieldCreatedAt).
//		Scan(ctx, &v)
func (ciq *ComicImgQuery) Select(fields ...string) *ComicImgSelect {
	ciq.ctx.Fields = append(ciq.ctx.Fields, fields...)
	sbuild := &ComicImgSelect{ComicImgQuery: ciq}
	sbuild.label = comicimg.Label
	sbuild.flds, sbuild.scan = &ciq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ComicImgSelect configured with the given aggregations.
func (ciq *ComicImgQuery) Aggregate(fns ...AggregateFunc) *ComicImgSelect {
	return ciq.Select().Aggregate(fns...)
}

func (ciq *ComicImgQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ciq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ciq); err != nil {
				return err
			}
		}
	}
	for _, f := range ciq.ctx.Fields {
		if !comicimg.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ciq.path != nil {
		prev, err := ciq.path(ctx)
		if err != nil {
			return err
		}
		ciq.sql = prev
	}
	return nil
}

func (ciq *ComicImgQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ComicImg, error) {
	var (
		nodes       = []*ComicImg{}
		withFKs     = ciq.withFKs
		_spec       = ciq.querySpec()
		loadedTypes = [1]bool{
			ciq.withChapter != nil,
		}
	)
	if ciq.withChapter != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, comicimg.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ComicImg).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ComicImg{config: ciq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	_spec.Node.Schema = ciq.schemaConfig.ComicImg
	ctx = internal.NewSchemaConfigContext(ctx, ciq.schemaConfig)
	if len(ciq.modifiers) > 0 {
		_spec.Modifiers = ciq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ciq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := ciq.withChapter; query != nil {
		if err := ciq.loadChapter(ctx, query, nodes, nil,
			func(n *ComicImg, e *ComicChapter) { n.Edges.Chapter = e }); err != nil {
			return nil, err
		}
	}
	for i := range ciq.loadTotal {
		if err := ciq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (ciq *ComicImgQuery) loadChapter(ctx context.Context, query *ComicChapterQuery, nodes []*ComicImg, init func(*ComicImg), assign func(*ComicImg, *ComicChapter)) error {
	ids := make([]pulid.ID, 0, len(nodes))
	nodeids := make(map[pulid.ID][]*ComicImg)
	for i := range nodes {
		if nodes[i].comic_chapter_imgs == nil {
			continue
		}
		fk := *nodes[i].comic_chapter_imgs
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(comicchapter.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "comic_chapter_imgs" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (ciq *ComicImgQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ciq.querySpec()
	_spec.Node.Schema = ciq.schemaConfig.ComicImg
	ctx = internal.NewSchemaConfigContext(ctx, ciq.schemaConfig)
	if len(ciq.modifiers) > 0 {
		_spec.Modifiers = ciq.modifiers
	}
	_spec.Node.Columns = ciq.ctx.Fields
	if len(ciq.ctx.Fields) > 0 {
		_spec.Unique = ciq.ctx.Unique != nil && *ciq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ciq.driver, _spec)
}

func (ciq *ComicImgQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(comicimg.Table, comicimg.Columns, sqlgraph.NewFieldSpec(comicimg.FieldID, field.TypeString))
	_spec.From = ciq.sql
	if unique := ciq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ciq.path != nil {
		_spec.Unique = true
	}
	if fields := ciq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, comicimg.FieldID)
		for i := range fields {
			if fields[i] != comicimg.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ciq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ciq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ciq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ciq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ciq *ComicImgQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ciq.driver.Dialect())
	t1 := builder.Table(comicimg.Table)
	columns := ciq.ctx.Fields
	if len(columns) == 0 {
		columns = comicimg.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ciq.sql != nil {
		selector = ciq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ciq.ctx.Unique != nil && *ciq.ctx.Unique {
		selector.Distinct()
	}
	t1.Schema(ciq.schemaConfig.ComicImg)
	ctx = internal.NewSchemaConfigContext(ctx, ciq.schemaConfig)
	selector.WithContext(ctx)
	for _, m := range ciq.modifiers {
		m(selector)
	}
	for _, p := range ciq.predicates {
		p(selector)
	}
	for _, p := range ciq.order {
		p(selector)
	}
	if offset := ciq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ciq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (ciq *ComicImgQuery) ForUpdate(opts ...sql.LockOption) *ComicImgQuery {
	if ciq.driver.Dialect() == dialect.Postgres {
		ciq.Unique(false)
	}
	ciq.modifiers = append(ciq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return ciq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (ciq *ComicImgQuery) ForShare(opts ...sql.LockOption) *ComicImgQuery {
	if ciq.driver.Dialect() == dialect.Postgres {
		ciq.Unique(false)
	}
	ciq.modifiers = append(ciq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return ciq
}

// Modify adds a query modifier for attaching custom logic to queries.
func (ciq *ComicImgQuery) Modify(modifiers ...func(s *sql.Selector)) *ComicImgSelect {
	ciq.modifiers = append(ciq.modifiers, modifiers...)
	return ciq.Select()
}

// ComicImgGroupBy is the group-by builder for ComicImg entities.
type ComicImgGroupBy struct {
	selector
	build *ComicImgQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (cigb *ComicImgGroupBy) Aggregate(fns ...AggregateFunc) *ComicImgGroupBy {
	cigb.fns = append(cigb.fns, fns...)
	return cigb
}

// Scan applies the selector query and scans the result into the given value.
func (cigb *ComicImgGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cigb.build.ctx, "GroupBy")
	if err := cigb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ComicImgQuery, *ComicImgGroupBy](ctx, cigb.build, cigb, cigb.build.inters, v)
}

func (cigb *ComicImgGroupBy) sqlScan(ctx context.Context, root *ComicImgQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(cigb.fns))
	for _, fn := range cigb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*cigb.flds)+len(cigb.fns))
		for _, f := range *cigb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*cigb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cigb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ComicImgSelect is the builder for selecting fields of ComicImg entities.
type ComicImgSelect struct {
	*ComicImgQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (cis *ComicImgSelect) Aggregate(fns ...AggregateFunc) *ComicImgSelect {
	cis.fns = append(cis.fns, fns...)
	return cis
}

// Scan applies the selector query and scans the result into the given value.
func (cis *ComicImgSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cis.ctx, "Select")
	if err := cis.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ComicImgQuery, *ComicImgSelect](ctx, cis.ComicImgQuery, cis, cis.inters, v)
}

func (cis *ComicImgSelect) sqlScan(ctx context.Context, root *ComicImgQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(cis.fns))
	for _, fn := range cis.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*cis.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cis.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (cis *ComicImgSelect) Modify(modifiers ...func(s *sql.Selector)) *ComicImgSelect {
	cis.modifiers = append(cis.modifiers, modifiers...)
	return cis
}
