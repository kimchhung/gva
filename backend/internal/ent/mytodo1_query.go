// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/gva/app/database/schema/xid"
	"github.com/gva/internal/ent/mytodo1"
	"github.com/gva/internal/ent/predicate"

	"github.com/gva/internal/ent/internal"
)

// MyTodo1Query is the builder for querying MyTodo1 entities.
type MyTodo1Query struct {
	config
	ctx        *QueryContext
	order      []mytodo1.OrderOption
	inters     []Interceptor
	predicates []predicate.MyTodo1
	loadTotal  []func(context.Context, []*MyTodo1) error
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the MyTodo1Query builder.
func (mt *MyTodo1Query) Where(ps ...predicate.MyTodo1) *MyTodo1Query {
	mt.predicates = append(mt.predicates, ps...)
	return mt
}

// Limit the number of records to be returned by this query.
func (mt *MyTodo1Query) Limit(limit int) *MyTodo1Query {
	mt.ctx.Limit = &limit
	return mt
}

// Offset to start from.
func (mt *MyTodo1Query) Offset(offset int) *MyTodo1Query {
	mt.ctx.Offset = &offset
	return mt
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (mt *MyTodo1Query) Unique(unique bool) *MyTodo1Query {
	mt.ctx.Unique = &unique
	return mt
}

// Order specifies how the records should be ordered.
func (mt *MyTodo1Query) Order(o ...mytodo1.OrderOption) *MyTodo1Query {
	mt.order = append(mt.order, o...)
	return mt
}

// First returns the first MyTodo1 entity from the query.
// Returns a *NotFoundError when no MyTodo1 was found.
func (mt *MyTodo1Query) First(ctx context.Context) (*MyTodo1, error) {
	nodes, err := mt.Limit(1).All(setContextOp(ctx, mt.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{mytodo1.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (mt *MyTodo1Query) FirstX(ctx context.Context) *MyTodo1 {
	node, err := mt.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first MyTodo1 ID from the query.
// Returns a *NotFoundError when no MyTodo1 ID was found.
func (mt *MyTodo1Query) FirstID(ctx context.Context) (id xid.ID, err error) {
	var ids []xid.ID
	if ids, err = mt.Limit(1).IDs(setContextOp(ctx, mt.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{mytodo1.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (mt *MyTodo1Query) FirstIDX(ctx context.Context) xid.ID {
	id, err := mt.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single MyTodo1 entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one MyTodo1 entity is found.
// Returns a *NotFoundError when no MyTodo1 entities are found.
func (mt *MyTodo1Query) Only(ctx context.Context) (*MyTodo1, error) {
	nodes, err := mt.Limit(2).All(setContextOp(ctx, mt.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{mytodo1.Label}
	default:
		return nil, &NotSingularError{mytodo1.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (mt *MyTodo1Query) OnlyX(ctx context.Context) *MyTodo1 {
	node, err := mt.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only MyTodo1 ID in the query.
// Returns a *NotSingularError when more than one MyTodo1 ID is found.
// Returns a *NotFoundError when no entities are found.
func (mt *MyTodo1Query) OnlyID(ctx context.Context) (id xid.ID, err error) {
	var ids []xid.ID
	if ids, err = mt.Limit(2).IDs(setContextOp(ctx, mt.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{mytodo1.Label}
	default:
		err = &NotSingularError{mytodo1.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (mt *MyTodo1Query) OnlyIDX(ctx context.Context) xid.ID {
	id, err := mt.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of MyTodo1s.
func (mt *MyTodo1Query) All(ctx context.Context) ([]*MyTodo1, error) {
	ctx = setContextOp(ctx, mt.ctx, "All")
	if err := mt.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*MyTodo1, *MyTodo1Query]()
	return withInterceptors[[]*MyTodo1](ctx, mt, qr, mt.inters)
}

// AllX is like All, but panics if an error occurs.
func (mt *MyTodo1Query) AllX(ctx context.Context) []*MyTodo1 {
	nodes, err := mt.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of MyTodo1 IDs.
func (mt *MyTodo1Query) IDs(ctx context.Context) (ids []xid.ID, err error) {
	if mt.ctx.Unique == nil && mt.path != nil {
		mt.Unique(true)
	}
	ctx = setContextOp(ctx, mt.ctx, "IDs")
	if err = mt.Select(mytodo1.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (mt *MyTodo1Query) IDsX(ctx context.Context) []xid.ID {
	ids, err := mt.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (mt *MyTodo1Query) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, mt.ctx, "Count")
	if err := mt.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, mt, querierCount[*MyTodo1Query](), mt.inters)
}

// CountX is like Count, but panics if an error occurs.
func (mt *MyTodo1Query) CountX(ctx context.Context) int {
	count, err := mt.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (mt *MyTodo1Query) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, mt.ctx, "Exist")
	switch _, err := mt.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (mt *MyTodo1Query) ExistX(ctx context.Context) bool {
	exist, err := mt.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the MyTodo1Query builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (mt *MyTodo1Query) Clone() *MyTodo1Query {
	if mt == nil {
		return nil
	}
	return &MyTodo1Query{
		config:     mt.config,
		ctx:        mt.ctx.Clone(),
		order:      append([]mytodo1.OrderOption{}, mt.order...),
		inters:     append([]Interceptor{}, mt.inters...),
		predicates: append([]predicate.MyTodo1{}, mt.predicates...),
		// clone intermediate query.
		sql:  mt.sql.Clone(),
		path: mt.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"createdAt,omitempty" rql:"filter,sort"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.MyTodo1.Query().
//		GroupBy(mytodo1.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (mt *MyTodo1Query) GroupBy(field string, fields ...string) *MyTodo1GroupBy {
	mt.ctx.Fields = append([]string{field}, fields...)
	grbuild := &MyTodo1GroupBy{build: mt}
	grbuild.flds = &mt.ctx.Fields
	grbuild.label = mytodo1.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"createdAt,omitempty" rql:"filter,sort"`
//	}
//
//	client.MyTodo1.Query().
//		Select(mytodo1.FieldCreatedAt).
//		Scan(ctx, &v)
func (mt *MyTodo1Query) Select(fields ...string) *MyTodo1Select {
	mt.ctx.Fields = append(mt.ctx.Fields, fields...)
	sbuild := &MyTodo1Select{MyTodo1Query: mt}
	sbuild.label = mytodo1.Label
	sbuild.flds, sbuild.scan = &mt.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a MyTodo1Select configured with the given aggregations.
func (mt *MyTodo1Query) Aggregate(fns ...AggregateFunc) *MyTodo1Select {
	return mt.Select().Aggregate(fns...)
}

func (mt *MyTodo1Query) prepareQuery(ctx context.Context) error {
	for _, inter := range mt.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, mt); err != nil {
				return err
			}
		}
	}
	for _, f := range mt.ctx.Fields {
		if !mytodo1.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if mt.path != nil {
		prev, err := mt.path(ctx)
		if err != nil {
			return err
		}
		mt.sql = prev
	}
	return nil
}

func (mt *MyTodo1Query) sqlAll(ctx context.Context, hooks ...queryHook) ([]*MyTodo1, error) {
	var (
		nodes = []*MyTodo1{}
		_spec = mt.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*MyTodo1).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &MyTodo1{config: mt.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	_spec.Node.Schema = mt.schemaConfig.MyTodo1
	ctx = internal.NewSchemaConfigContext(ctx, mt.schemaConfig)
	if len(mt.modifiers) > 0 {
		_spec.Modifiers = mt.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, mt.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	for i := range mt.loadTotal {
		if err := mt.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (mt *MyTodo1Query) sqlCount(ctx context.Context) (int, error) {
	_spec := mt.querySpec()
	_spec.Node.Schema = mt.schemaConfig.MyTodo1
	ctx = internal.NewSchemaConfigContext(ctx, mt.schemaConfig)
	if len(mt.modifiers) > 0 {
		_spec.Modifiers = mt.modifiers
	}
	_spec.Node.Columns = mt.ctx.Fields
	if len(mt.ctx.Fields) > 0 {
		_spec.Unique = mt.ctx.Unique != nil && *mt.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, mt.driver, _spec)
}

func (mt *MyTodo1Query) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(mytodo1.Table, mytodo1.Columns, sqlgraph.NewFieldSpec(mytodo1.FieldID, field.TypeString))
	_spec.From = mt.sql
	if unique := mt.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if mt.path != nil {
		_spec.Unique = true
	}
	if fields := mt.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, mytodo1.FieldID)
		for i := range fields {
			if fields[i] != mytodo1.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := mt.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := mt.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := mt.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := mt.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (mt *MyTodo1Query) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(mt.driver.Dialect())
	t1 := builder.Table(mytodo1.Table)
	columns := mt.ctx.Fields
	if len(columns) == 0 {
		columns = mytodo1.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if mt.sql != nil {
		selector = mt.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if mt.ctx.Unique != nil && *mt.ctx.Unique {
		selector.Distinct()
	}
	t1.Schema(mt.schemaConfig.MyTodo1)
	ctx = internal.NewSchemaConfigContext(ctx, mt.schemaConfig)
	selector.WithContext(ctx)
	for _, m := range mt.modifiers {
		m(selector)
	}
	for _, p := range mt.predicates {
		p(selector)
	}
	for _, p := range mt.order {
		p(selector)
	}
	if offset := mt.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := mt.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (mt *MyTodo1Query) ForUpdate(opts ...sql.LockOption) *MyTodo1Query {
	if mt.driver.Dialect() == dialect.Postgres {
		mt.Unique(false)
	}
	mt.modifiers = append(mt.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return mt
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (mt *MyTodo1Query) ForShare(opts ...sql.LockOption) *MyTodo1Query {
	if mt.driver.Dialect() == dialect.Postgres {
		mt.Unique(false)
	}
	mt.modifiers = append(mt.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return mt
}

// Modify adds a query modifier for attaching custom logic to queries.
func (mt *MyTodo1Query) Modify(modifiers ...func(s *sql.Selector)) *MyTodo1Select {
	mt.modifiers = append(mt.modifiers, modifiers...)
	return mt.Select()
}

// MyTodo1GroupBy is the group-by builder for MyTodo1 entities.
type MyTodo1GroupBy struct {
	selector
	build *MyTodo1Query
}

// Aggregate adds the given aggregation functions to the group-by query.
func (mtb *MyTodo1GroupBy) Aggregate(fns ...AggregateFunc) *MyTodo1GroupBy {
	mtb.fns = append(mtb.fns, fns...)
	return mtb
}

// Scan applies the selector query and scans the result into the given value.
func (mtb *MyTodo1GroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, mtb.build.ctx, "GroupBy")
	if err := mtb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MyTodo1Query, *MyTodo1GroupBy](ctx, mtb.build, mtb, mtb.build.inters, v)
}

func (mtb *MyTodo1GroupBy) sqlScan(ctx context.Context, root *MyTodo1Query, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(mtb.fns))
	for _, fn := range mtb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*mtb.flds)+len(mtb.fns))
		for _, f := range *mtb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*mtb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := mtb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// MyTodo1Select is the builder for selecting fields of MyTodo1 entities.
type MyTodo1Select struct {
	*MyTodo1Query
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (mt *MyTodo1Select) Aggregate(fns ...AggregateFunc) *MyTodo1Select {
	mt.fns = append(mt.fns, fns...)
	return mt
}

// Scan applies the selector query and scans the result into the given value.
func (mt *MyTodo1Select) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, mt.ctx, "Select")
	if err := mt.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MyTodo1Query, *MyTodo1Select](ctx, mt.MyTodo1Query, mt, mt.inters, v)
}

func (mt *MyTodo1Select) sqlScan(ctx context.Context, root *MyTodo1Query, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(mt.fns))
	for _, fn := range mt.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*mt.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := mt.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (mt *MyTodo1Select) Modify(modifiers ...func(s *sql.Selector)) *MyTodo1Select {
	mt.modifiers = append(mt.modifiers, modifiers...)
	return mt
}
