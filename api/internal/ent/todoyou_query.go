// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/kimchhung/gva/internal/ent/predicate"
	"github.com/kimchhung/gva/internal/ent/todoyou"
)

// TodoYouQuery is the builder for querying TodoYou entities.
type TodoYouQuery struct {
	config
	ctx        *QueryContext
	order      []todoyou.OrderOption
	inters     []Interceptor
	predicates []predicate.TodoYou
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the TodoYouQuery builder.
func (tyq *TodoYouQuery) Where(ps ...predicate.TodoYou) *TodoYouQuery {
	tyq.predicates = append(tyq.predicates, ps...)
	return tyq
}

// Limit the number of records to be returned by this query.
func (tyq *TodoYouQuery) Limit(limit int) *TodoYouQuery {
	tyq.ctx.Limit = &limit
	return tyq
}

// Offset to start from.
func (tyq *TodoYouQuery) Offset(offset int) *TodoYouQuery {
	tyq.ctx.Offset = &offset
	return tyq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (tyq *TodoYouQuery) Unique(unique bool) *TodoYouQuery {
	tyq.ctx.Unique = &unique
	return tyq
}

// Order specifies how the records should be ordered.
func (tyq *TodoYouQuery) Order(o ...todoyou.OrderOption) *TodoYouQuery {
	tyq.order = append(tyq.order, o...)
	return tyq
}

// First returns the first TodoYou entity from the query.
// Returns a *NotFoundError when no TodoYou was found.
func (tyq *TodoYouQuery) First(ctx context.Context) (*TodoYou, error) {
	nodes, err := tyq.Limit(1).All(setContextOp(ctx, tyq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{todoyou.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (tyq *TodoYouQuery) FirstX(ctx context.Context) *TodoYou {
	node, err := tyq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first TodoYou ID from the query.
// Returns a *NotFoundError when no TodoYou ID was found.
func (tyq *TodoYouQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = tyq.Limit(1).IDs(setContextOp(ctx, tyq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{todoyou.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (tyq *TodoYouQuery) FirstIDX(ctx context.Context) int {
	id, err := tyq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single TodoYou entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one TodoYou entity is found.
// Returns a *NotFoundError when no TodoYou entities are found.
func (tyq *TodoYouQuery) Only(ctx context.Context) (*TodoYou, error) {
	nodes, err := tyq.Limit(2).All(setContextOp(ctx, tyq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{todoyou.Label}
	default:
		return nil, &NotSingularError{todoyou.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (tyq *TodoYouQuery) OnlyX(ctx context.Context) *TodoYou {
	node, err := tyq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only TodoYou ID in the query.
// Returns a *NotSingularError when more than one TodoYou ID is found.
// Returns a *NotFoundError when no entities are found.
func (tyq *TodoYouQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = tyq.Limit(2).IDs(setContextOp(ctx, tyq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{todoyou.Label}
	default:
		err = &NotSingularError{todoyou.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (tyq *TodoYouQuery) OnlyIDX(ctx context.Context) int {
	id, err := tyq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of TodoYous.
func (tyq *TodoYouQuery) All(ctx context.Context) ([]*TodoYou, error) {
	ctx = setContextOp(ctx, tyq.ctx, "All")
	if err := tyq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*TodoYou, *TodoYouQuery]()
	return withInterceptors[[]*TodoYou](ctx, tyq, qr, tyq.inters)
}

// AllX is like All, but panics if an error occurs.
func (tyq *TodoYouQuery) AllX(ctx context.Context) []*TodoYou {
	nodes, err := tyq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of TodoYou IDs.
func (tyq *TodoYouQuery) IDs(ctx context.Context) (ids []int, err error) {
	if tyq.ctx.Unique == nil && tyq.path != nil {
		tyq.Unique(true)
	}
	ctx = setContextOp(ctx, tyq.ctx, "IDs")
	if err = tyq.Select(todoyou.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (tyq *TodoYouQuery) IDsX(ctx context.Context) []int {
	ids, err := tyq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (tyq *TodoYouQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, tyq.ctx, "Count")
	if err := tyq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, tyq, querierCount[*TodoYouQuery](), tyq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (tyq *TodoYouQuery) CountX(ctx context.Context) int {
	count, err := tyq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (tyq *TodoYouQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, tyq.ctx, "Exist")
	switch _, err := tyq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (tyq *TodoYouQuery) ExistX(ctx context.Context) bool {
	exist, err := tyq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the TodoYouQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (tyq *TodoYouQuery) Clone() *TodoYouQuery {
	if tyq == nil {
		return nil
	}
	return &TodoYouQuery{
		config:     tyq.config,
		ctx:        tyq.ctx.Clone(),
		order:      append([]todoyou.OrderOption{}, tyq.order...),
		inters:     append([]Interceptor{}, tyq.inters...),
		predicates: append([]predicate.TodoYou{}, tyq.predicates...),
		// clone intermediate query.
		sql:  tyq.sql.Clone(),
		path: tyq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"createdAt,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.TodoYou.Query().
//		GroupBy(todoyou.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (tyq *TodoYouQuery) GroupBy(field string, fields ...string) *TodoYouGroupBy {
	tyq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &TodoYouGroupBy{build: tyq}
	grbuild.flds = &tyq.ctx.Fields
	grbuild.label = todoyou.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"createdAt,omitempty"`
//	}
//
//	client.TodoYou.Query().
//		Select(todoyou.FieldCreatedAt).
//		Scan(ctx, &v)
func (tyq *TodoYouQuery) Select(fields ...string) *TodoYouSelect {
	tyq.ctx.Fields = append(tyq.ctx.Fields, fields...)
	sbuild := &TodoYouSelect{TodoYouQuery: tyq}
	sbuild.label = todoyou.Label
	sbuild.flds, sbuild.scan = &tyq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a TodoYouSelect configured with the given aggregations.
func (tyq *TodoYouQuery) Aggregate(fns ...AggregateFunc) *TodoYouSelect {
	return tyq.Select().Aggregate(fns...)
}

func (tyq *TodoYouQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range tyq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, tyq); err != nil {
				return err
			}
		}
	}
	for _, f := range tyq.ctx.Fields {
		if !todoyou.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if tyq.path != nil {
		prev, err := tyq.path(ctx)
		if err != nil {
			return err
		}
		tyq.sql = prev
	}
	return nil
}

func (tyq *TodoYouQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*TodoYou, error) {
	var (
		nodes = []*TodoYou{}
		_spec = tyq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*TodoYou).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &TodoYou{config: tyq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, tyq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (tyq *TodoYouQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := tyq.querySpec()
	_spec.Node.Columns = tyq.ctx.Fields
	if len(tyq.ctx.Fields) > 0 {
		_spec.Unique = tyq.ctx.Unique != nil && *tyq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, tyq.driver, _spec)
}

func (tyq *TodoYouQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(todoyou.Table, todoyou.Columns, sqlgraph.NewFieldSpec(todoyou.FieldID, field.TypeInt))
	_spec.From = tyq.sql
	if unique := tyq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if tyq.path != nil {
		_spec.Unique = true
	}
	if fields := tyq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, todoyou.FieldID)
		for i := range fields {
			if fields[i] != todoyou.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := tyq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := tyq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := tyq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := tyq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (tyq *TodoYouQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(tyq.driver.Dialect())
	t1 := builder.Table(todoyou.Table)
	columns := tyq.ctx.Fields
	if len(columns) == 0 {
		columns = todoyou.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if tyq.sql != nil {
		selector = tyq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if tyq.ctx.Unique != nil && *tyq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range tyq.predicates {
		p(selector)
	}
	for _, p := range tyq.order {
		p(selector)
	}
	if offset := tyq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := tyq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// TodoYouGroupBy is the group-by builder for TodoYou entities.
type TodoYouGroupBy struct {
	selector
	build *TodoYouQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (tygb *TodoYouGroupBy) Aggregate(fns ...AggregateFunc) *TodoYouGroupBy {
	tygb.fns = append(tygb.fns, fns...)
	return tygb
}

// Scan applies the selector query and scans the result into the given value.
func (tygb *TodoYouGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, tygb.build.ctx, "GroupBy")
	if err := tygb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TodoYouQuery, *TodoYouGroupBy](ctx, tygb.build, tygb, tygb.build.inters, v)
}

func (tygb *TodoYouGroupBy) sqlScan(ctx context.Context, root *TodoYouQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(tygb.fns))
	for _, fn := range tygb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*tygb.flds)+len(tygb.fns))
		for _, f := range *tygb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*tygb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tygb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// TodoYouSelect is the builder for selecting fields of TodoYou entities.
type TodoYouSelect struct {
	*TodoYouQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (tys *TodoYouSelect) Aggregate(fns ...AggregateFunc) *TodoYouSelect {
	tys.fns = append(tys.fns, fns...)
	return tys
}

// Scan applies the selector query and scans the result into the given value.
func (tys *TodoYouSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, tys.ctx, "Select")
	if err := tys.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TodoYouQuery, *TodoYouSelect](ctx, tys.TodoYouQuery, tys, tys.inters, v)
}

func (tys *TodoYouSelect) sqlScan(ctx context.Context, root *TodoYouQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(tys.fns))
	for _, fn := range tys.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*tys.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tys.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
