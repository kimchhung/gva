// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"time"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/kimchhung/gva/internal/ent/todome"
)

// TodoMeCreate is the builder for creating a TodoMe entity.
type TodoMeCreate struct {
	config
	mutation *TodoMeMutation
	hooks    []Hook
}

// SetCreatedAt sets the "created_at" field.
func (tmc *TodoMeCreate) SetCreatedAt(t time.Time) *TodoMeCreate {
	tmc.mutation.SetCreatedAt(t)
	return tmc
}

// SetNillableCreatedAt sets the "created_at" field if the given value is not nil.
func (tmc *TodoMeCreate) SetNillableCreatedAt(t *time.Time) *TodoMeCreate {
	if t != nil {
		tmc.SetCreatedAt(*t)
	}
	return tmc
}

// SetUpdatedAt sets the "updated_at" field.
func (tmc *TodoMeCreate) SetUpdatedAt(t time.Time) *TodoMeCreate {
	tmc.mutation.SetUpdatedAt(t)
	return tmc
}

// SetNillableUpdatedAt sets the "updated_at" field if the given value is not nil.
func (tmc *TodoMeCreate) SetNillableUpdatedAt(t *time.Time) *TodoMeCreate {
	if t != nil {
		tmc.SetUpdatedAt(*t)
	}
	return tmc
}

// SetName sets the "name" field.
func (tmc *TodoMeCreate) SetName(s string) *TodoMeCreate {
	tmc.mutation.SetName(s)
	return tmc
}

// Mutation returns the TodoMeMutation object of the builder.
func (tmc *TodoMeCreate) Mutation() *TodoMeMutation {
	return tmc.mutation
}

// Save creates the TodoMe in the database.
func (tmc *TodoMeCreate) Save(ctx context.Context) (*TodoMe, error) {
	tmc.defaults()
	return withHooks(ctx, tmc.sqlSave, tmc.mutation, tmc.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (tmc *TodoMeCreate) SaveX(ctx context.Context) *TodoMe {
	v, err := tmc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (tmc *TodoMeCreate) Exec(ctx context.Context) error {
	_, err := tmc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (tmc *TodoMeCreate) ExecX(ctx context.Context) {
	if err := tmc.Exec(ctx); err != nil {
		panic(err)
	}
}

// defaults sets the default values of the builder before save.
func (tmc *TodoMeCreate) defaults() {
	if _, ok := tmc.mutation.CreatedAt(); !ok {
		v := todome.DefaultCreatedAt()
		tmc.mutation.SetCreatedAt(v)
	}
	if _, ok := tmc.mutation.UpdatedAt(); !ok {
		v := todome.DefaultUpdatedAt()
		tmc.mutation.SetUpdatedAt(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (tmc *TodoMeCreate) check() error {
	if _, ok := tmc.mutation.CreatedAt(); !ok {
		return &ValidationError{Name: "created_at", err: errors.New(`ent: missing required field "TodoMe.created_at"`)}
	}
	if _, ok := tmc.mutation.UpdatedAt(); !ok {
		return &ValidationError{Name: "updated_at", err: errors.New(`ent: missing required field "TodoMe.updated_at"`)}
	}
	if _, ok := tmc.mutation.Name(); !ok {
		return &ValidationError{Name: "name", err: errors.New(`ent: missing required field "TodoMe.name"`)}
	}
	return nil
}

func (tmc *TodoMeCreate) sqlSave(ctx context.Context) (*TodoMe, error) {
	if err := tmc.check(); err != nil {
		return nil, err
	}
	_node, _spec := tmc.createSpec()
	if err := sqlgraph.CreateNode(ctx, tmc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	tmc.mutation.id = &_node.ID
	tmc.mutation.done = true
	return _node, nil
}

func (tmc *TodoMeCreate) createSpec() (*TodoMe, *sqlgraph.CreateSpec) {
	var (
		_node = &TodoMe{config: tmc.config}
		_spec = sqlgraph.NewCreateSpec(todome.Table, sqlgraph.NewFieldSpec(todome.FieldID, field.TypeInt))
	)
	if value, ok := tmc.mutation.CreatedAt(); ok {
		_spec.SetField(todome.FieldCreatedAt, field.TypeTime, value)
		_node.CreatedAt = value
	}
	if value, ok := tmc.mutation.UpdatedAt(); ok {
		_spec.SetField(todome.FieldUpdatedAt, field.TypeTime, value)
		_node.UpdatedAt = value
	}
	if value, ok := tmc.mutation.Name(); ok {
		_spec.SetField(todome.FieldName, field.TypeString, value)
		_node.Name = value
	}
	return _node, _spec
}

// TodoMeCreateBulk is the builder for creating many TodoMe entities in bulk.
type TodoMeCreateBulk struct {
	config
	err      error
	builders []*TodoMeCreate
}

// Save creates the TodoMe entities in the database.
func (tmcb *TodoMeCreateBulk) Save(ctx context.Context) ([]*TodoMe, error) {
	if tmcb.err != nil {
		return nil, tmcb.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(tmcb.builders))
	nodes := make([]*TodoMe, len(tmcb.builders))
	mutators := make([]Mutator, len(tmcb.builders))
	for i := range tmcb.builders {
		func(i int, root context.Context) {
			builder := tmcb.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*TodoMeMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, tmcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, tmcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, tmcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (tmcb *TodoMeCreateBulk) SaveX(ctx context.Context) []*TodoMe {
	v, err := tmcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (tmcb *TodoMeCreateBulk) Exec(ctx context.Context) error {
	_, err := tmcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (tmcb *TodoMeCreateBulk) ExecX(ctx context.Context) {
	if err := tmcb.Exec(ctx); err != nil {
		panic(err)
	}
}
