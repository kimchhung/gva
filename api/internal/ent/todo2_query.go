// Code generated by ent, DO NOT EDIT.

package ent

import (
	"gva/internal/ent/predicate"
	"gva/internal/ent/todo2"
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// Todo2Query is the builder for querying Todo2 entities.
type Todo2Query struct {
	config
	ctx        *QueryContext
	order      []todo2.OrderOption
	inters     []Interceptor
	predicates []predicate.Todo2
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the Todo2Query builder.
func (t *Todo2Query) Where(ps ...predicate.Todo2) *Todo2Query {
	t.predicates = append(t.predicates, ps...)
	return t
}

// Limit the number of records to be returned by this query.
func (t *Todo2Query) Limit(limit int) *Todo2Query {
	t.ctx.Limit = &limit
	return t
}

// Offset to start from.
func (t *Todo2Query) Offset(offset int) *Todo2Query {
	t.ctx.Offset = &offset
	return t
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (t *Todo2Query) Unique(unique bool) *Todo2Query {
	t.ctx.Unique = &unique
	return t
}

// Order specifies how the records should be ordered.
func (t *Todo2Query) Order(o ...todo2.OrderOption) *Todo2Query {
	t.order = append(t.order, o...)
	return t
}

// First returns the first Todo2 entity from the query.
// Returns a *NotFoundError when no Todo2 was found.
func (t *Todo2Query) First(ctx context.Context) (*Todo2, error) {
	nodes, err := t.Limit(1).All(setContextOp(ctx, t.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{todo2.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (t *Todo2Query) FirstX(ctx context.Context) *Todo2 {
	node, err := t.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Todo2 ID from the query.
// Returns a *NotFoundError when no Todo2 ID was found.
func (t *Todo2Query) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = t.Limit(1).IDs(setContextOp(ctx, t.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{todo2.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (t *Todo2Query) FirstIDX(ctx context.Context) int {
	id, err := t.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Todo2 entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Todo2 entity is found.
// Returns a *NotFoundError when no Todo2 entities are found.
func (t *Todo2Query) Only(ctx context.Context) (*Todo2, error) {
	nodes, err := t.Limit(2).All(setContextOp(ctx, t.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{todo2.Label}
	default:
		return nil, &NotSingularError{todo2.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (t *Todo2Query) OnlyX(ctx context.Context) *Todo2 {
	node, err := t.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Todo2 ID in the query.
// Returns a *NotSingularError when more than one Todo2 ID is found.
// Returns a *NotFoundError when no entities are found.
func (t *Todo2Query) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = t.Limit(2).IDs(setContextOp(ctx, t.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{todo2.Label}
	default:
		err = &NotSingularError{todo2.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (t *Todo2Query) OnlyIDX(ctx context.Context) int {
	id, err := t.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Todo2s.
func (t *Todo2Query) All(ctx context.Context) ([]*Todo2, error) {
	ctx = setContextOp(ctx, t.ctx, "All")
	if err := t.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Todo2, *Todo2Query]()
	return withInterceptors[[]*Todo2](ctx, t, qr, t.inters)
}

// AllX is like All, but panics if an error occurs.
func (t *Todo2Query) AllX(ctx context.Context) []*Todo2 {
	nodes, err := t.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Todo2 IDs.
func (t *Todo2Query) IDs(ctx context.Context) (ids []int, err error) {
	if t.ctx.Unique == nil && t.path != nil {
		t.Unique(true)
	}
	ctx = setContextOp(ctx, t.ctx, "IDs")
	if err = t.Select(todo2.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (t *Todo2Query) IDsX(ctx context.Context) []int {
	ids, err := t.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (t *Todo2Query) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, t.ctx, "Count")
	if err := t.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, t, querierCount[*Todo2Query](), t.inters)
}

// CountX is like Count, but panics if an error occurs.
func (t *Todo2Query) CountX(ctx context.Context) int {
	count, err := t.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (t *Todo2Query) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, t.ctx, "Exist")
	switch _, err := t.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (t *Todo2Query) ExistX(ctx context.Context) bool {
	exist, err := t.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the Todo2Query builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (t *Todo2Query) Clone() *Todo2Query {
	if t == nil {
		return nil
	}
	return &Todo2Query{
		config:     t.config,
		ctx:        t.ctx.Clone(),
		order:      append([]todo2.OrderOption{}, t.order...),
		inters:     append([]Interceptor{}, t.inters...),
		predicates: append([]predicate.Todo2{}, t.predicates...),
		// clone intermediate query.
		sql:  t.sql.Clone(),
		path: t.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Todo2.Query().
//		GroupBy(todo2.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (t *Todo2Query) GroupBy(field string, fields ...string) *Todo2GroupBy {
	t.ctx.Fields = append([]string{field}, fields...)
	grbuild := &Todo2GroupBy{build: t}
	grbuild.flds = &t.ctx.Fields
	grbuild.label = todo2.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.Todo2.Query().
//		Select(todo2.FieldCreatedAt).
//		Scan(ctx, &v)
func (t *Todo2Query) Select(fields ...string) *Todo2Select {
	t.ctx.Fields = append(t.ctx.Fields, fields...)
	sbuild := &Todo2Select{Todo2Query: t}
	sbuild.label = todo2.Label
	sbuild.flds, sbuild.scan = &t.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a Todo2Select configured with the given aggregations.
func (t *Todo2Query) Aggregate(fns ...AggregateFunc) *Todo2Select {
	return t.Select().Aggregate(fns...)
}

func (t *Todo2Query) prepareQuery(ctx context.Context) error {
	for _, inter := range t.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, t); err != nil {
				return err
			}
		}
	}
	for _, f := range t.ctx.Fields {
		if !todo2.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if t.path != nil {
		prev, err := t.path(ctx)
		if err != nil {
			return err
		}
		t.sql = prev
	}
	return nil
}

func (t *Todo2Query) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Todo2, error) {
	var (
		nodes = []*Todo2{}
		_spec = t.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Todo2).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Todo2{config: t.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, t.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (t *Todo2Query) sqlCount(ctx context.Context) (int, error) {
	_spec := t.querySpec()
	_spec.Node.Columns = t.ctx.Fields
	if len(t.ctx.Fields) > 0 {
		_spec.Unique = t.ctx.Unique != nil && *t.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, t.driver, _spec)
}

func (t *Todo2Query) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(todo2.Table, todo2.Columns, sqlgraph.NewFieldSpec(todo2.FieldID, field.TypeInt))
	_spec.From = t.sql
	if unique := t.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if t.path != nil {
		_spec.Unique = true
	}
	if fields := t.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, todo2.FieldID)
		for i := range fields {
			if fields[i] != todo2.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := t.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := t.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := t.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := t.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (t *Todo2Query) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(t.driver.Dialect())
	t1 := builder.Table(todo2.Table)
	columns := t.ctx.Fields
	if len(columns) == 0 {
		columns = todo2.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if t.sql != nil {
		selector = t.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if t.ctx.Unique != nil && *t.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range t.predicates {
		p(selector)
	}
	for _, p := range t.order {
		p(selector)
	}
	if offset := t.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := t.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Todo2GroupBy is the group-by builder for Todo2 entities.
type Todo2GroupBy struct {
	selector
	build *Todo2Query
}

// Aggregate adds the given aggregation functions to the group-by query.
func (tb *Todo2GroupBy) Aggregate(fns ...AggregateFunc) *Todo2GroupBy {
	tb.fns = append(tb.fns, fns...)
	return tb
}

// Scan applies the selector query and scans the result into the given value.
func (tb *Todo2GroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, tb.build.ctx, "GroupBy")
	if err := tb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*Todo2Query, *Todo2GroupBy](ctx, tb.build, tb, tb.build.inters, v)
}

func (tb *Todo2GroupBy) sqlScan(ctx context.Context, root *Todo2Query, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(tb.fns))
	for _, fn := range tb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*tb.flds)+len(tb.fns))
		for _, f := range *tb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*tb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Todo2Select is the builder for selecting fields of Todo2 entities.
type Todo2Select struct {
	*Todo2Query
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (t *Todo2Select) Aggregate(fns ...AggregateFunc) *Todo2Select {
	t.fns = append(t.fns, fns...)
	return t
}

// Scan applies the selector query and scans the result into the given value.
func (t *Todo2Select) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, t.ctx, "Select")
	if err := t.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*Todo2Query, *Todo2Select](ctx, t.Todo2Query, t, t.inters, v)
}

func (t *Todo2Select) sqlScan(ctx context.Context, root *Todo2Query, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(t.fns))
	for _, fn := range t.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*t.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := t.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
